## 끝낸 일
- c++20 모듈시스템을 IDE나 다른 환경들이 제대로 지원안해서 intellisence가 안됨. 컴파일러 cl->clang 으로 변경.
- 디버거도 변경. 그래도 제대로 작동안해서 매번 language server재시작. (vscode의 c++확장판은 문제 없어보이나 windsurf는 라이선스문제로 vscode c++확장판 못설치.)


......

- 스크롤 윈도우가 표시되는 패널에 바운딩박스를 보여줄지 안보여줄지 결정하는 체크박스컨트롤을 추가하여 표시/비표시를 제어할 수 있도록 함. (완료)

- 스크롤 윈도우가 OnPaint메서드를 만날때 계속 이미지 copy가 일어나는 것을 확인할 수 있음. 만약 바운딩박스를 보여줄 필요가 없으면 copy가 일어나지 않고 캐싱된 기본 이미지를 보여주어야함. (완료)

- Controller의 GetDisplayImage는 디스플레이 포맷과 저장된 ImageData의 포맷이 다를경우 지속적으로 변환하고 카피함. 이것을 캐싱하여 불필요한 변환과 카피를 줄일 수 있도록 개선해야함. (완료)

- 스크롤윈도우가 이미지업데이트하는 과정 리팩토링해야함. (완료) 컨트롤러가 없거나 id가 없는 경우를 가정하고 복제가 발생하고 그외 복제가 자꾸 일어나는경우가있음. 체크하고 불필요한 과정 전부 삭제.(완료)

- updatedisplay메서드가 언제 호출되는지 알아야함. 불필요한 복제 다수 의심. 화면을 갱신하는 메서드가 OnPaint, Refresh가 있어서 더 복잡해짐. (완료, 해당메서드삭제함)

- SetBoundingBoxDisplayMode에서 캐시가 항상 꺼짐으로 바뀜. 어째서? 처음 이미지를 로드할 때 이미 원본데이터를 가지고 (완료)있는데다가 해당 원본데이터를 한번만 디스플레이하기위해 변환한 이후에 캐시로 저장하면 되는데... (완료)

- 컨트롤러의 GetDisplayImage 리팩토링. 해당 메서드에서 바운딩박스 그리기와 이미지변환이 같이 이루어짐. 이 두가지 로직을 분리하여 캐시를 효율적으로 사용할 수 있도록 개선해야함. (완료)

- wx위젯에 이미지를 그리기위해 최종적으로 wxBitmap으로 변환해야함. 이 과정에서 캐시를 사용하여 불필요한 변환을 줄일 수 있도록 개선해야함. 스크롤윈도우가 원본이미지말고도 변환된 wxBitmap도 가지고있을것. 포맷을 직관적으로 알 수 있도록 멤버변수이름에 _wxbitmap suffix를 붙여야함. (완료)

- 저장된 바운딩박스를 순서대로 좌표값과 함께 보여주는 리스트박스를 추가하기. (완료)

- 옵저버패턴으로 메인프레임과 스크롤윈도우가 통신하지 않고 바운딩박스변화에따라 렌더링 다시함. (완료)

- 바운딩박스뷰에서 선택된 아이템 삭제버튼과 기능구현. (완료)

- 사용자로 부터 입력받은 x,y,w,h 값을 통해 바운딩 박스를 추가할 수 있는 '바운딩 박스 추가' 버튼을 추가하기. (완료)

- 사용자로부터 입력 받은 x,y,w,h 값을 버튼을 통해 수정할 수 있도록 8개의 버튼을 추가. 각 버튼은 값을 크게 또는 작게 함. (값의 변동 범위를 설정할 수 있도록 사용자로부터 입력 받는 컨트롤 추가. 기본값은 1) (완료)

- 레이아웃 수정하기: 바운딩박스 관련 UI를 통합해서 관리할 수 있게 하고 버튼의 배치나 크기를 조절해야 함. (UI패널들 분리 - 메인, 스크롤윈도우, 박스패널)

- 레이아웃 망가짐 (왼쪽 이미지윈도우와 우측의 패널비율이 제대로 적용이 안되는문제)
- wxWidget은 알고보니 레이아웃배치가 굉장히 비직관적임. 결함있는 라이브러리인듯.
- 스크롤윈도우가 최소크기를 갖지않으면 굉장히 작은 영역을 가지는 문제 (다른컨트롤들이 expand해서 자리를 차지함.)를 해결하기 위해 스크롤윈도우 바깥에 패널을 추가해서 expand할 수 있게 했지만 여전히 망가짐. 플러터는 이 방법으로 해결이 되었지만 이 라이브러리는 결함이 있는건지 내가 문법을 제대로 이해를 못했는건지 모르겠음. sizer라는것이 패널도 가질 수 있고 다른 사이저도 가질 수 있다는데 다른 사이저를 줬을경우 메모리접근 에러가남. 
내가 라이브러리에 이해가 부족해서인듯. 다시 수정방법을 찾기.(AI SetSizer를 빼먹은듯)
SetSizer를 빼먹은것 보단 AI의 코드가 Sizer에 Sizer를 더하면서 제대로 연결이 되어지지않아 의도한 대로 움직이지 않았음. (notebook클래스의 동작방식이 문제인듯, 바로 아래의 사이저와 연결되어야하는듯.)
스크롤윈도우와 수평사이저를 같이 배치할 경우 스크롤윈도우가 제대로 늘어나지 않는 것을 확인함. UI가 다른 최신형 gui와 다른듯.

- 바운딩박스를 선택하면 이미지 상에서 강조되어 그려질 수 있도록 해야 함. (완료)

-박스 선택을 옵저버패턴으로 각 gui가 변경되도록 함. 현재는
박스패널과 메인프레임이 강하게연결. (이미지 ID저장을 메인프레임이 해서 그런듯.) (완료)
옵저버패턴으로 전환해도 박스패널에서 ID값을 알기위해 지속적으로 컨트롤러에 요청해야하는데 그냥 ID 값을 갖도록 상태등록하는게 나을지 고민.
-무료 제미니가 바로 찾는 원인을 계속 못찾음.

- GUI 패널들 분리하는것 마무리, 옵저버패턴 강화(보낸클래스 검증.) (완료)
- 옵저버 처리함수 강화 (보낸클래스 확인 및 패널이 스스로 값 변경할 때도 같이 사용할 수 있게함.) (완료)
- 패널이 자신의 상태를 변경해야 하는경우는(ex. 유저입력) 하나 하나 컨트롤의 변경마다 컨트롤러에 접근하는 것이 아니고 모든 상태변경을 마친 이후에 컨트롤러에 접근할 수 있도록 해야함. 중간에 컨트롤러에 접근 하더라도 값만 받아오고 상태변경중이니 컨트롤러가 업데이트 알림을 발생시키지 않도록 해야함. (컨트롤러의 get만 쓰면 가능?) (완료)
- 패널의 컨트롤이 사용자로부터 입력을 받은 경우 바인딩된 각 함수들이 변경을하고 컨트롤러를 호출하는데, 이때 옵저버신호처리함수와 중복되는 로직이 다수 발생하기에 바인딩된 함수 내용을 옵저버신호처리함수에 통합하기. (패널의 각 바인딩된 함수에 중간함수로 분리하는 것은 코드를 복잡하고 지나치게 길게 만들기 때문에 최대한 피하기.) (완료) 



