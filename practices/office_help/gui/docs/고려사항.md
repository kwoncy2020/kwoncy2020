## 아래의 결론 : 코드가 지나치게 복잡해지고 현재 대부분의 컨트롤들은 간단한처리만 담당하기에 불가피할때만 옵저버처리함수재사용하기.

2-3. 스크롤윈도우에서 마우스 좌 더블클릭시 하이라이트가 되지 않는 문제를 AI는 계속 못찾음. 자꾸 아니라는데 캐시문제라고 그러기만함. 아마도 더블클릭시 컨트롤러에 박스인덱스를 셋하라고 호출함. 그 이후 컨트롤러는 옵저버로 알림. 그런데 박스인덱스를 셋하고 난 이후에 하이라이트로 그리는 과정이 없는듯. 좌 더블클릭시 컨트롤러의 -> SetSelectedBoundingBoxIndex 메서드 호출함. 여기서 컨트롤러는 바로 다른 옵저버에게 알리기만하지 그리는 이미지를 준비안함??? 그런데 컨트롤러는 인덱스가 셋 되어있으면 항상 박스그려진 이미지를 요청받을때 하이라이트되도록 그릴텐데;; 디버깅하니 아에 더블클릭 이벤트가 제대로 생성이안되는듯. 우 더블클릭은 됨. (AI가 좌 더블클릭을 지웠거나 했는듯)
2-4. 다른 바운딩박스를 바운딩박스뷰안에서 선택하면 박스 하이라이트는 다르게 그려지지만 그 후 박스리스브튜에서 포커스가 사라짐.
2-5. 컨트롤러를 팩토리패턴을 적용해서 단일인스턴스화해야할듯. 각 패널 분리이후 추가한 콜백을 모두 제거하고 옵저버패턴으로 각 패널이 컨트롤러에만 의존하도록 변경.
2-6. 패털들 분리완료. 패널사이 의존선 제거완료. 옵저버패턴으로 컨트롤러와 단일통신. 현재 스크롤윈도우에서 추가한 바운딩 박스가 박스패널에 제대로 추가가 안되는중.
2-7. 옵저버 패턴을 쓰면서 재귀적으로 신호를 주고받는 경우가 생길 수 있으므로 컨트롤러가 신호를 줬던 쪽에서 값을 변경하고 다시 변경을 알리는 경우에는 해당 패널에 다시 변경을 알리지 않고 다른 패널에만 알리도록 함.
2-8. 위의 방법은 컨트롤러와 패널의 1:1무한재귀를 막을 순 있겠으나 여러 패널들이 컨트롤러를 통해 신호를 주고받으면서 같은 컨트롤을 수정하면서 무한 루프가 생김. 패널은 항상 컨트롤러로부터 받은 신호의 경우 소비만하고 다시 컨트롤러에게 변경 신호를 발생시키지 않는다면..? 유저의 입력으로부터 변경된 것과 컨트롤의 신호에 의해 변경된 것을 구분한다면...  유저에 의해 값의 변경 --> 패널이 컨트롤러에게 신호발생 --> 컨트롤러는 해당 패널을 제외한 나머지에게 신호발생 --> 나머지 패널들은 유저의 입력이 아닌 컨트롤러의 신호이므로 값의 갱신만 하고 다시 신호를 발생시키지 않음. 이렇게 한다면 무한루프나 중복수정이 발생하지 않는가??
2-9 하지만 문제가 한가지 더 있는듯. 하나의 패널 안에서 일부 컨트롤의 값이 변할때 다른 컨트롤에도 영향이 있을 수 있으니 내부적으로 업데이트신호를 발생시키는데 이것이 컨트롤러를 호출 할 수 있음. 맞음. 하나의 패널에 여러개의 컨트롤이 서로 의존한다면 컨트롤러를 호출해 값의 변경을 알리고, 컨트롤러가 다시 호출을 하면서 나머지 값들을 변경하는데..... 그렇다면 패널 스스로가 자신의 값을 업데이트하는 함수를 호출하는 경우.... 업데이트함수에 다른 컨트롤들이 컨트롤러에 신호를 보내지 못하게하는 플래그를 추가해야함.. 그러고 모든 업데이트가 마치게한뒤 업데이트함수를 호출한 최초의 컨트롤이 컨트롤러에게 업데이트 신호를 보내야함. 각 패널은 옵저버신호를처리하는 함수를 강화하여 자신의 내용을 업데이트하도록 하고 모든 업데이트가 마쳐야 컨트롤러에게 값의 변경을 알려서 컨트롤러가 다른 패널을 업데이트하게 만들어야함. (하지만 구조가 복잡해지고 기능이 많아질수록 각 패널별로 추가적으로 상태를 저장하지 않는한은 컨트롤러에 의존해야하고 그렇게되면 한 패널안에서 컨트롤러에게 접근하지않으면서 필요한 모든 상태변화를 할 수 없음.)
(하지만 한번의 유저의 요청이 여러번의 컨트롤러의 접근을 유발한다면 그것도 잘못된 설계일듯. 한 패널의 변경사항이 발생했을때 컨트롤러에 한 번만 접근할 수 있어야함.) 
2-10 옵저버 처리함수에는 자신이 컨트롤러에게 보낸 신호에의해 다시 업데이트 하지 안도록 신호를 보내는 패널 자신의 값을 파라미터로 추가해야함.(this)(타입변경문제와 클래스 메모리주소등 문제로 this전달 및 검증은 안좋은 구조.) 또한 처리신호가 컨트롤러에서 왔는지 검증을해서 신호를 다시 발생시킬지 여부를 결정해야하는데 이건 컨트롤러에서 this값을 주면 될듯.(X)

2-11 옵저버패턴에 방문자패턴 추가하기. 이것으로 컨트롤러에서 알람을보낼때 매번 자신에게 데이터변경을 알렸던 패널을 제외하기위해 옵저버들의 타입을 조사할 필요가 없어짐.
2-12 방문자는 내 경우가 아닌듯. 타입조사는 enum클래스에 각 패널과 컨트롤러타입을 추가해서 비교하면될듯.
2-13 한 패널내에서 여러가지업데이트가 발생해 패널의 업데이트가 끝난후 컨트롤러에게 여러번 업데이트를 알려야하는데.... 함수를 담은 컨테이너를 이용...?